services:
  web:
    build:
      context: https://github.com/pipe3/u-checker.git
    ports:
      - "5000:5000"
    volumes:
      - ${STACK_DIR}/data:/data
    environment:
      DATA_DIR: /data
    restart: unless-stopped

