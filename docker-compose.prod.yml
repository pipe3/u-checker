services:
  web:
    build:
      context: https://github.com/pipe3/u-checker.git
    ports:
      - "5000:5000"
    volumes:
      - ${STACK_DIR}/data:/data
    env_file:
      - ${STACK_DIR}/u-checker.env
    environment:
      DATA_DIR: /data
    restart: unless-stopped

